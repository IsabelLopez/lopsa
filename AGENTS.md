# AGENTS.md — sitio web de LOPSA (lopsa.com.pa)

Las reglas de este repositorio están en [`CLAUDE.md`](CLAUDE.md) y **valen igual para cualquier agente**
(Codex/ChatGPT, Claude u otro), no solo para Claude. Léelo completo antes de cambiar nada: qué archivo se edita
para cada cosa, el orden para publicar (rama → pull request → merge a `main` = lopsa.com.pa) y las reglas de
contenido que no se negocian (sin RUC ni dirección exacta, sin precios ni marcas, garantía con su texto exacto).

Si cambias una regla, cámbiala en `CLAUDE.md`, no aquí: este archivo solo apunta a ese.
