# Este repositorio contiene el sitio estático de lopsa.com.pa (raíz) y, en legacy/, el «Coming Soon» de Isabel.
# dist/ SÍ se versiona: es lo que Netlify publica, sin necesidad de construir nada en el servidor.

__pycache__/
*.pyc
.netlify/
.env*
*.local.*

# el proyecto Next.js heredado nunca se instala dentro del repositorio
legacy/**/node_modules/
legacy/**/.next/
legacy/**/out/

# borradores y capturas locales
_capturas/
_borradores/
